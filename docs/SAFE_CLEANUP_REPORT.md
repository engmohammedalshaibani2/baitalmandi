# SAFE_CLEANUP_REPORT.md

## 1. مقدمة
بناءً على توجيهات العمل في وضع **SAFE REFACTOR MODE**، يُمنع الحذف المباشر لأي دالة أو ملف يُشتبه في عدم استخدامه، وذلك لتفادي حدوث أي انحدار (Regression) في بيئة الإنتاج. تم حصر كافة العناصر غير المستخدمة وتوثيقها أدناه.

---

## 2. Unused Imports Cleaned (العناصر التي تم تنظيفها بالفعل)
تم فحص وإزالة الاستيرادات غير المستخدمة التالية لتجنب تحذيرات المترجم:
- **الملف**: `src/app/admin/orders/actions.ts`
  - **الاستيراد المحذوف**: `canAccessPage` من `@/lib/permissions` (غير مستخدمة محلياً في هذا الملف).

---

## 3. SAFE_TO_DELETE_CANDIDATES (مرشحون للحذف الآمن)
الملفات والبرمجيات التالية لا تؤثر على تشغيل التطبيق في بيئة الإنتاج ويمكن حذفها مستقبلاً بعد مراجعة المسؤول:

### أ. ملفات الفحص والاختبار (Scratch Files)
1. **الملف**: `test-db.ts` (في جذر المشروع)
   - **السبب**: ملف مستقل لتجربة الاتصال بقاعدة البيانات.
   - **عدد المراجع**: 0.
2. **الملف**: `test-rls.ts` (في جذر المشروع)
   - **السبب**: برمجية تجريبية للتحقق من سياسات RLS وقواعد الأمان.
   - **عدد المراجع**: 0.

### ب. ملفات SQL والتوثيق
1. **الملف**: `src/db/rls.sql`
   - **السبب**: يحتوي على مرجع لسياسات RLS بصيغة SQL. يتم تطبيق السياسات مباشرةً في Supabase أو عبر Migrations. لا يتم استيراده في كود التطبيق الفعلي.
   - **عدد المراجع**: 0 (يستخدم للتوثيق فقط).

### ج. مرشحو الدوال والمنطق المكرر
1. **الدالة**: `isDeveloper` في `src/lib/permissions.ts`
   - **السبب**: تستخدم فقط في صفحة الإعدادات، وإذا تم دمج رتبة المطور بالكامل تحت رتبة المدير العام `manager` أو `admin` مستقبلاً، فلن تكون هناك حاجة إليها.
   - **عدد المراجع**: 2 (في `src/app/admin/settings/ClientPage.tsx` و `src/actions/settings.ts`).

---

## 4. التوصيات للأمان والصلابة
- **إجراء مراجعة دورية**: مراجعة دورية للملفات ذات المراجع الصفرية (0) في جذر المشروع وحذفها لتنظيف المستودع.
- **تجنب دمج ملفات الـ SQL**: نقل ملفات التوثيق مثل `src/db/rls.sql` إلى مستودع أو مجلد مستقل خارج كود المخطط البرمجي الفعلي (مثل مجلد `supabase/migrations`) لضمان وضوح بنية الكود.
